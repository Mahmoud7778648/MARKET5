import { createTheme } from "@mui/material/styles";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#352b2b ",
    },
  },
});

export default darkTheme; // تأكد من وجود هذا السطر
